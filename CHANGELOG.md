# Changelog

## 0.15.0

- 增加 `trajectory` 子命令：由带标注的 Agent 轨迹 JSONL 生成作弊、退化与等义三族变体，输出仍是 `score` 的输入格式，评分、审计、对比与来源标记全部沿用既有流程；
- **审的是给轨迹打分的评分器，不是 Agent 本身**；不产出 Agent 排行榜，与既有定位一致；
- 三族变体的后置条件第一次**严格可判定**：作弊变体去掉重复项后须与基准逐项相等，退化变体须恰好删除或掏空标注步骤且其余按原序保留，等义变体的步骤多重集须与基准完全相同而顺序不同；
- **退化变体一律保留最终答案**：同时改变过程与结果就无法区分评分器在读过程还是在读结论。结论一字未改而取得结论的那一步没了，评分器照给高分即为失效证据；
- 作弊变体只重复 Agent 真正做过的调用，绝不捏造工具名或调用结果——那是伪造证据；
- 承重步骤与可换序分组均须人工标注，工具不推断；缺标注、序号越界、覆盖全部步骤、分组少于两个成员或成员跨组重叠，一律报错退出；
- `hollow_evidence` 拒绝掏空一个本来就没有结果的步骤：空串是任何串的子串，「旧结果已消失」的检查在这种情况下会空洞地通过；
- 轨迹标签与空结果标记接入既有 `LanguageStrategy` 语言接缝，新增语言无需改渲染器；
- 生成清单记录 `modality: "trajectory"` 与逐变体的干预明细；不进入生成指纹身份字段，避免旧清单无法计算指纹；
- 增加 `examples/trajectory_baselines.jsonl`（五个案例）与 `examples/trajectory_rubric.md`；
- **删除一处不可达断言**：`remove_load_bearing_step` 的步骤数校验永远不会触发，因为逐项比对已蕴含长度相等。无法被任何输入触发的断言给出的是虚假保证；
- 两个生成器重复的「拒绝覆盖案例文件」守卫合并为一个函数：同一条拒绝写两份必然漂移，漂移后会在其中一处悄悄停止拒绝；
- `hollow_evidence` 的空结果守卫同样合并为一条：提前拒绝与断言子句守同一件事，前者先触发就让后者永远无从检验，效果等同不可达；
- 变异体增至74个，全部被捕获；完整测试数增至647项。

## 0.14.0

- 增加 `paraphrase` 子命令：调用模型起草等义改写，只写待审文件，绝不触碰案例集；`generate` 因此保持纯离线、确定性、零依赖；
- 改写提示只含原文与「保持含义不变地重写」，不含评分标准、用途说明或变体类型标签——针对被测维度优化过的改写是从侧门进来的循环论证；
- 机器检查分为硬拦与提示两层。数字与否定词检查**实测会拦下本仓库五个人工真·等义改写中的2个**（「睡眠不足」→「缺乏睡眠」、「统一」里的「一」），因此降级为 `review_notes`，不影响 `status`；40%的误拦率会训练审阅者忽略该列，比没有检查更糟；
- `generate --append --paraphrase-review` 合并 `status=approved` 的行；`status` 取值非法或 `baseline_sha256` 与当前基准不符即报错并指名案例；
- **批准行标记集合为 `mixed`**：等义的判断来自人而非工具可验证的后置条件，来源标记必须如实反映这一点；重复合并不产生重复行；
- 生成清单记录 `paraphrase_model`；**改写模型与评分模型完全相同时审计直接拒绝**，否则等于让被测系统自己定义「什么算等义」；报告并列印出两个模型名，同族不同版本无法自动判别，交由读者判断；
- `mixed` 集合现在也可以提供生成清单：指纹仍然校验，只是不再据此断言机器生成。此前它被一律拒绝，导致混合集合完全无法回溯到生成运行；
- 与之配套，**声明 `human-authored` 却提供了与评分案例指纹相符的生成清单一律拒绝**：那份指纹恰好证明这些行出自生成器。旧逻辑只拦住 `mixed` 清单，反而放过了更露骨的矛盾组合；
- 抽出共用传输层：重试、URL校验与错误处理此前埋在评分器内部，改写器需要同一套逻辑，两份实现必然漂移；
- **修复变异门的一处数据破坏**：Windows上一次 `OSError` 发生在 `finally` 的恢复写入中，异常越过了「是否已全部恢复」的检查，进程带着仍被变异的 `agent_audit/scoring.py` 崩溃退出。现在所有写入都带重试且不抛出，恢复校验无条件执行，并在失败时逐行打印 `git checkout --` 命令；
- 增加行尾不变量测试：`.gitattributes` 声明 `eol=lf`，而变异门按字节匹配锚点，一个被改成CRLF的源文件会让其中全部变异体静默失效；
- 变异体增至60个，全部被捕获；完整测试数增至533项。

## 0.13.0

- 增加英文语言策略：`--language chinese|english`，默认中文，不做自动检测；
- 英文分句处理缩写（`Dr.` `e.g.`）、小数（`3.5`）、人名首字母（`J. K.`）与小写续行；朴素按句号切分会多切出句子，而标注是按句序号给的，多切一句就会让其后每个序号都指向错误的句子；
- 增加可选的 `sentence_count` 列：填了就校验分句结果，不等即报错并打印实际切分与所用分句器，把静默切错变成大声拒绝；
- 增加 `--show-sentences`：打印每个基准的编号分句与标注，供标注前后核对；
- 连接词替换增加逐语言的词边界规则——英文必须整词匹配（否则 `Finally` 会匹配到 `Finalised`），中文绝不能用这条（中文字符 `isalpha()` 为真，加了会让替换全部失效）；
- **修复英文变体句子黏连**：追加的语料会和原文直接相连（`...back to 8:30.This is obviously...`），产出不合语法的英文；现按语言使用句间分隔符；
- 增加英文示例基准 `examples/essay_baselines_en.csv`，五个案例的分句与标注均经 `sentence_count` 校验；
- 变异体增至47个，全部被捕获；完整测试数增至456项。

## 0.12.0

- **修复数据丢失缺陷**：`generate` 曾在输出文件已存在时静默覆盖，而README明确邀请用户手工替换生成的行，等于邀请用户把工作做进一个随时会被抹掉的文件。现在已存在即拒绝，与检查点的做法一致；
- 增加 `--append`：把新变体合并进已有案例文件，逐行保留已有内容（包括手工编辑），只补缺失的行；
- 合并时逐行与「生成器本应产出的文本」比对，任何被编辑的行或手工新增的变体都会把整份集合标记为 `mixed`；
- 清单增加 `set_origin` 与 `merge` 分类计数（preserved / appended / edited / foreign）；
- **审计拒绝把 `mixed` 集合声明为 `machine-generated`**，否则追加功能会把手工内容洗白成上一版刚建立的可验证声明；
- 笔记与正文一同参与哈希，因此只改笔记同样会把集合降级为 `mixed`；
- 生成命令的提示不再自相矛盾：集合为 `mixed` 时不再建议声明 `machine-generated`；
- 变异体增至43个，全部被捕获；完整测试数增至420项。

## 0.11.0

- 生成清单记录 `output_sha256`，与 `score` 计算输入哈希的口径完全一致；
- `audit` 增加 `--generation-manifest`，比对该哈希与评分清单的 `input_sha256`，不等即说明案例在生成后被改动，直接报错并提示改用 `mixed`；
- `--variant-origin machine-generated` 现在**必须**提供生成清单，使该声明成为可验证的事实而不是用户打字打上去的一句话；
- 审计JSON的 `comparison_context` 记录 `generation_sha256`，补上从审计结果回溯到生成运行的链条；
- `compare` 要求两侧生成指纹一致，一侧有一侧无也拒绝；旧审计JSON无该字段时两侧同为空仍可比较；
- 把 `stable_hash` 提到 `io` 模块，生成与评分共用同一份定义，而不是两份碰巧一致的实现；
- 变异体增至39个，全部被捕获；完整测试数增至400项。

## 0.10.0

- 增加 `generate` 子命令：由带标注的基准生成作弊与内容退化变体，输出直接就是 `score` 的输入格式；
- 退化变体只依据人工标注的承重句生成，工具不推断；缺标注、序号越界或标注覆盖全部句子时报错退出；
- 每个变体必须通过机器可验证的后置条件才写出：作弊变体必须完整包含基准作为前缀，退化变体必须不含任何标注句且按原序保留其余句子；
- 生成清单逐变体记录干预幅度与实际插入的语料原文，便于人工确认「无关」前提成立；
- 同一输入与同一种子产出逐字节相同的结果；
- 等义改写默认关闭：保守替换几乎必然通过，掺入会稀释违规率；开启后只在小句边界替换，遇到无可替换连接词的文本直接拒绝；
- 修复中文朴素替换的切词陷阱：「原因此外」实为「原因+此外」，无条件替换会产出非中文，现只在句首或逗号后替换；
- 修复空断言可绕过后置条件的缺陷：Python 中空串是任何串的子串，`in` 检查形同虚设；
- 增加 `segmentation` 与 `variants` 两个模块及带标注的示例基准 `examples/essay_baselines.csv`；
- 变异体增至36个，全部被捕获；完整测试数增至381项，行覆盖率99.5%。

## 0.9.0

- 增加 `--variant-origin`，声明变体由人工撰写、工具生成还是混合产生；
- 机器生成的变体在Markdown与HTML报告中明确标注为下限测试：未通过是确凿证据，通过不能证明系统可靠；
- `compare` 拒绝变体来源不一致的两份审计，避免拿人工变体的基准去比机器变体的候选；
- 旧审计JSON没有该字段时读取为空，两侧均为空仍可比较，不修改 `schema_version`；
- 覆盖率工具改为递归发现模块，避免后续新增子包时静默不计入覆盖；
- 变异体增至30个，覆盖比较器的来源一致性检查与两处下限测试提示；
- 增加自动变体生成的设计说明与新的 `docs/specs/` 分类；
- 完整测试数增至298项。

## 0.8.4

- 用CI实测数据校准覆盖率预算：Windows/3.12、Ubuntu/3.12 与 Ubuntu/3.10 三套环境的未覆盖行数逐模块完全一致，均为14行，只有百分比相差0.1个点；
- 预算由估计的「实测值加2行」收紧为「实测值加1行」，并在注释中记录校准依据与适用解释器；
- 该结果实证了把逐模块门槛由百分比改为行数上限的判断：行数跨解释器稳定，百分比不稳定。

## 0.8.3

- 修复覆盖率门禁在Linux上漏测 `agent_audit/io.py` 的问题：`trace` 的忽略判断按文件基名缓存，标准库 `io.py` 被忽略后我们的同名模块一并被丢弃，且先后顺序依平台而定，导致同一条命令在Windows报告10个模块、在Linux只报告9个；
- 覆盖率门禁改为直接驱动 `trace.Trace`，并以绝对路径判断是否记录，不再依赖模块名或 `.cover` 文件命名；
- 修复测试发现发生在追踪区域之外导致模块级代码与 `def` 行不被计入的问题，新旧两套实现在同一解释器上得出完全一致的数字；
- 将逐模块门槛由百分比改为未覆盖行数上限：解释器之间对可执行行的归属有一两行差异，百分比会把这点差异按模块大小放大；
- 门禁新增两条不变量：任何模块零记录直接失败，任何模块缺少预算直接失败；
- 覆盖 `python -m agent_audit` 入口，该入口写在README里却从未被执行过；
- 增加覆盖率工具自身的测试，固定「与标准库同名的模块仍必须被记录」和「判断与提问顺序无关」；
- 在发布检查中增加确认远端CI通过的步骤；此前三个版本在本地全绿的情况下连续推送，而CI一直是红的；
- 完整测试数由275项增至286项。

## 0.8.2

- 覆盖长任务检查点的完整性拒绝面：创建冲突、缺失文件、空文件、空行、非对象行、缺失或错误的头部、不支持的schema、上下文不一致、重复样本，以及逐字段的样本校验；
- 覆盖被篡改的检查点：头部哈希仍然匹配但混入了本次运行之外的样本，只有逐样本范围检查能拦下；
- 覆盖评分适配器的传输层：可重试状态码逐个验证、终态客户端错误不重试、重试耗尽后报错、非JSON响应、缺失回复体、用量统计缺失与连接被拒；
- 覆盖CLI的参数装配、子命令分派与评分清单校验，清单是版本对比合法性的证据链起点；
- 覆盖 `run_scoring` 的全部参数校验与评分器返回值校验，包括布尔分数与畸形令牌计数；
- 修正一处凭错误原因通过的检查点测试，它撞上的是上下文不一致而非样本越界；
- 将单个未联通端点测试的超时由2秒降至0.25秒，测试套件由8.7秒回到3.5秒；
- 变异体由20个增至27个，新增检查点、清单、重试与CSV有限性等拒绝面，全部被捕获；
- `agent_audit` 行覆盖率由89.2%升至99.4%，`io`、`report`、`html_report`、`models`、`scoring` 达到100%；
- 完整测试数由158项增至275项。

## 0.8.1

- 增加变异测试门禁 `scripts/mutation_check.py`，以20个可能真实发生的缺陷验证测试是否真的能区分正确与错误行为；
- 门禁立即发现「效度余量小于等于0即判HIGH」这条README主规则没有任何测试覆盖，删除该判据后全部测试仍通过，现已补测；
- 增加比较器失败关闭测试：评分上下文、阈值、量表、来源声明、变体集合与类型、逐变体字段类型、汇总数字篡改与JSON装载错误；
- 修正一处名称与断言不符的测试，该用例实际验证的是高违规率单独即可判HIGH；
- 修正测试中 `margin or 0.0` 的假值陷阱，效度余量恰为0时会被误读为缺失；
- `comparison` 行覆盖率由88.0%升至99.3%，整体由86.9%升至89.2%，完整测试数由108项增至158项；
- 变异门禁接入发布检查与CI，本地审查门保持快速。

## 0.8.0

- 修复非有限分数在公开API上失败打开的问题：NaN基准分曾使全部变体判定为不违规并报告LOW风险，现在直接拒绝；
- 修复自由度超过30时改用1.96的反保守区间，超出列表范围后保持最保守的t值，避免把临界判定显示为确定；
- 修复评分适配器把布尔值当作分数的问题：`true` 曾被静默记为1.0分、`false` 记为0.0分，现在按非数值拒绝；
- 增加独立重演已公布公式的属性测试，对随机输入交叉检验效度余量与违规计数；
- 增加三类判定的阈值边界测试，明确落在阈值上的变体不算违规；
- 增加配置、记录与CSV的失败关闭测试，覆盖此前从未执行过的拒绝分支；
- 增加评分适配器对代码块、夹杂散文、非JSON、数组、越界分数和非本机HTTP端点的处理测试；
- 增加证据等级三档与HTML临界徽标的渲染测试，断言改为定位计算结果所在行；
- 增加仅依赖标准库的覆盖率门禁脚本与逐模块棘轮下限，并接入本地审查与CI；
- 使用15个变异体验证新测试确实能捕获对应缺陷，全部捕获；
- 完整测试数由43项增至108项，`agent_audit` 行覆盖率86.9%，报告渲染模块达到100%。

## 0.7.0

- 增加英文项目入口，覆盖方法、快速开始、恢复、版本对比、公开证据和安全边界；
- 增加可提交的合成HTML报告与确定性构建脚本，便于直接下载和离线展示；
- 增加公开资产测试，验证HTML示例与当前渲染器完全一致，并检查相对Markdown链接；
- 增加隐私安全的GitHub审计需求模板，明确禁止在公开Issue提交敏感材料；
- 增加中英文作品集、简历和面试表述，并限定所有公开结论的证据边界；
- 增加仓库与发布规范，明确目录职责、生成物边界、文件命名和单一贡献者身份审查；
- 将文档拆分为示例、案例、服务、作品集和治理五类，并增加统一导航；
- 修复连续长系统名在移动端撑宽页面的问题；
- 增加公开需求模板链接与文档导航覆盖的一致性测试，防止公开入口和分类静默失效；
- 将Markdown、JSON与HTML产物统一写为LF行尾，使公开示例在不同操作系统上逐字节一致；
- 在目录职责表中登记 `.github/`，并把命名例外限定为顶层语言入口和GitHub约定路径；
- 完整测试数增至43项。

## 0.6.0

- 为 `audit` 与 `compare` 增加可选 `--html`，生成单文件、自包含的客户交付报告；
- 报告不依赖JavaScript、外部字体或网络资源，并通过内容安全策略限制可加载内容；
- 对系统名、案例ID、模型说明等不可信文本统一进行HTML转义；
- 增加响应式与打印样式，详细表格在窄屏保持可读宽度并支持横向滚动；
- 增加HTML注入、内容完整性和两条CLI端到端测试，完整测试数增至39项；
- 使用真实Gemma审计与版本对比结果完成桌面及移动端浏览器视觉审查，并修复窄屏表格逐字符换行问题。

## 0.5.0

- 增加可选 `--checkpoint`，每完成一次模型调用就同步保存一条可恢复记录；
- 增加 `--resume`，只调用检查点中尚未完成的样本；
- 检查点绑定输入、评分标准、模型、端点、量表、温度和重复次数，任何不一致都会拒绝恢复；
- 新运行拒绝覆盖已有检查点，避免静默丢失历史结果；
- 仅在完整上下文和已有样本均验证通过后，修复末尾未写完的JSON片段；
- 运行清单增加新调用数、恢复数、墙钟时间、样本延迟和令牌汇总；
- 原始轨迹增加单样本延迟与是否来自检查点；
- 增加7项故障注入与恢复测试，完整测试数增至37项；
- 使用本地Gemma 3验证20条样本完整恢复：新增调用0，恢复前后评分CSV完全一致。

## 0.4.0

- 增加 `compare` 命令，对两份审计JSON生成版本差异Markdown与JSON；
- 比较前强制校验案例、变体类型、阈值和评分量表完全一致；
- 审计JSON关联评分清单中的输入哈希、评分标准哈希、温度和重复次数；
- 将各类变体统一转换为“正数表示变差”的严重度变化；
- 区分跨阈值回归、跨阈值改善、阈值内变化和无变化；
- 任一侧缺少不确定性估计或属于临界判定时，将该项变化标为暂定；
- 审计与对比JSON增加 `schema_version`；
- 增加10项比较测试，完整测试数增至30项；
- 完成Gemma 3 4B与Gemma 4 E4B各60次本地调用的同条件对比示范。

## 0.3.0

- 增加 `--repeats`，对每条输入执行1–100次独立评分；
- 在评分CSV中保存均值、样本标准差与样本数；
- 原始JSONL轨迹记录每次采样序号，运行清单记录重复次数；
- 使用均值差标准误和保守95% t 区间识别临界判定；
- 未覆盖完整不确定性或存在临界判定时，将风险等级明确标为暂定；
- 增加重复采样、CSV往返、临界区间和端到端回归测试；
- 默认忽略 `outputs/` 下的所有产物，避免误提交评分文本或原始模型回复；
- 完成5个基准案例、60次本地Gemma 3 4B调用的重复采样示范。

## 0.2.0

- 增加OpenAI-compatible `/chat/completions` 评分适配器；
- API密钥只通过环境变量读取；
- 非本机HTTP端点会被拒绝；
- 增加输入与评分标准哈希、运行清单和可选原始响应记录；
- 增加评分范围与审计配置校验；
- 严重风险阈值根据评分量表跨度自动缩放；
- 强制每个案例包含基准、作弊和内容退化的成对测试；
- 少于5个基准案例时，将报告明确标记为探索性证据；
- 增加本地模拟API端到端测试和GitHub Actions。
- 增加可重复运行的本地审查脚本和质量门说明。

## 0.1.0

- 增加CSV离线审计；
- 支持作弊式修改、内容退化和等义改写；
- 生成Markdown与JSON审计结果；
- 增加合成示例、客户表单、服务说明和报告模板。
